# accounts
user_name: "{{app_name}}"
group_name: "{{app_name}}"
# paths
deploy_root: "/opt/{{app_name}}"
celery_bin: "{{deploy_root}}/.env/bin/celery"
src_root: "{{deploy_root}}/src"
unix_socket_name: "/tmp/{{app_name}}-wsgi.sock"
# nginx
http_port: 80
# misc
webapp_archive_location: /tmp/src.tar.gz
# required packages
required_packages:
    - rsync
    - python-virtualenv
    - python-dev
    - software-properties-common
    - libpq-dev
