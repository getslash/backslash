<small><a class="clickable" {{action "toggleModal"}} class="text-muted">Search help...</a></small>

{{#if display.show_help}}

  {{#modal-dialog
     onClose="toggleModal"
     targetAttachment="center"
     translucentOverlay=true}}
    <div class="container">
      <h3>Search Syntax Help</h3>
      <div class="container">
	{{yield}}
      </div>
    </div>
  {{/modal-dialog}}
{{/if}}
