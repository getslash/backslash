<div class="info">
  {{expand-toggle expanded=(mut expanded)}} {{moment-time unix=error.timestamp}} ({{moment-time ago=error.timestamp}}): <span class="mono">{{error.exception_type}}</span> {{#copy-button clipboardText=error.message title="Copy exception message to clipboard"}}<i class="fa fa-copy"></i>{{/copy-button}}
</div>

<div class="message mono"> {{if expanded error.message error.abbreviated_message}} </div>

{{#if expanded}}
  {{error-detail error=error}}{{else}}
{{/if}}
