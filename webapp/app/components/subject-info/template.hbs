{{#each subjects as |subject index|}}
  {{#if index}}
    ,
  {{else}}
    <i class="fa fa-rocket"></i>
  {{/if}}
  <strong>{{#link-to 'subject' subject.name bubbles=false}}{{subject.name}}{{/link-to}}</strong><span class="label-fine">{{subject.version}}</span>
{{/each}}

