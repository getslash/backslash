{{#tool-bar}}
  {{filter-control filter_config=filter_config filter_action=(action this.attrs.filter_action)}}
{{/tool-bar}}


{{#each results as |result|}}
  {{#if (eq result.type "test")}}
    {{query-result-test test=result}}
  {{/if}}

{{/each}}

{{pagination-nav page=page pages_total=pages_total num_visible=num_visible goto_page=(action this.attrs.goto_page)}}
