{{#tool-bar}}
  {{filter-control filter_config=filter_config update_filter=(action this.attrs.update_filter)}}
{{/tool-bar}}


{{#each results as |result|}}
  {{component (concat result.type "-item") item=result}}
{{/each}}

{{pagination-nav page=page pages_total=pages_total num_visible=num_visible goto_page=(action this.attrs.goto_page)}}
