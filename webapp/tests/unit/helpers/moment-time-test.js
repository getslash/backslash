import { momentTime } from '../../../helpers/moment-time';
import { module, test } from 'qunit';

module('Unit | Helper | moment time');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = momentTime(42);
  assert.ok(result);
});
