import { gravatarUrl } from '../../../helpers/gravatar-url';
import { module, test } from 'qunit';

module('Unit | Helper | gravatar url');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = gravatarUrl(42);
  assert.ok(result);
});
